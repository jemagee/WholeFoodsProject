<h1><%= @region.name %></h1>

<%= link_to fa_icon("edit", text: "Edit Region"), edit_region_path(@region), class: "btn btn-primary" %>
<%= link_to fa_icon("delete", text: "Delete Region"), region_path(@region), method: :delete, data: {confirm: "Are you sure you want to delete this region?"}, class: "btn btn-danger" %>

