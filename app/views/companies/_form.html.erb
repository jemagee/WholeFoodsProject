<% if company.errors.any? %>
	<%= render "shared/errors", {object: company} %>
<% end %>
<%= form_for(company) do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.submit "#{method} Company" %>
<% end %>