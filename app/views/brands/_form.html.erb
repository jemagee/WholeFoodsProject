<%= form_for brand do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :prefix %>
	<%= f.number_field :prefix %>

	<%= f.label :company %>
	<%= f.collection_select(:company_id, Company.all, :id, :name, prompt: true) %>

	<%= f.submit "#{action} Brand" %>
<% end %>